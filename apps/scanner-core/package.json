{
  "name": "scanner-core",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "cargo run",
    "test": "cargo test",
    "lint": "cargo clippy"
  }
}
